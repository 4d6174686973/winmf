# Channel subset

Wählt Kanäle entsprechend dem unter ``Channel subset`` definierten Kanalvektor aus. Die Syntax eines Kanalvektors ist unter  [Channel subset syntax](/de/help/edit/channel-subset-syntax) beschrieben. Nicht existierende Kanäle werden ignoriert.