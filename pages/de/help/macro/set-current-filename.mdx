# Set Current File Name

Hier kann ein neuer Filename eingegeben werden. Eine Ausführung von ``Save`` oder ``Load current filename`` verwendet dann diesen Namen im aktuellen Pfad des QC-Prozesses.

Wenn ``Add serial number from process control`` aktiviert ist, wird an den Namen automatisch die aktuelle Seriennummer des QC-Prozesses gehängt.
