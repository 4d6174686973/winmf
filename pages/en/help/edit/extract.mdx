# Extract

``Extract cuts`` a segment out of a time signal and discards the rest. The area to be cut out is defined in [Select region](/en/help/edit/region-control).

``Extract`` always affects all channels at once, as all channels of a time signal are required to have the same length.
