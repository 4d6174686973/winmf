# Set cursor distance

This function sets the inactive cursor to a position at a certain distance behind the active cursor. The distance is defined similarly to [signal length](/en/help/edit/signal-length).
