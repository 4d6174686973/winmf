# Extend

``Extend`` increases the length of a time signal by appending zeros at the end.

The total length of the new signal needs to be specified as described in [Signal length](/en/help/edit/signal-length). The value entered must exceed the length of the original signal.
