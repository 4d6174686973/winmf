# Differentiate

This function produces the derivative of a signal with respect to time or with respect to frequency for a spectrum correspondingly.

By ``Select channel`` the channel to be processed is chosen.

``Apply to all channels`` executes the function for all channels.
