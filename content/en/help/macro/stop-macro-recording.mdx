# Stop macro recording

The current macro recording is terminated. The macro file can now be loaded and launched by ``Play macro``.

[Macro definitions and macro editing](/en/help/macro/macro-definitions)