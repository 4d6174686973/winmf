# Cyclic move

``Cyclic move`` bewirkt eine zyklische Verschiebung eines Zeitsignals. Dabei wird das Sample, an dem der aktive Cursor steht, an den Anfang verschoben, der Bereich vor dem aktiven Cursor wird am Ende angehängt.

Mit ``Select channel`` wird der zu bearbeitende Kanal ausgewählt.

``Apply to all channels`` lässt die Funktion auf alle Kanäle wirken.