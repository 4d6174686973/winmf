# Region

Unter ``Region`` kann ausgewählt werden, ob der Bereich zwischen den Cursorn oder ein fester Bereich (``fixed``) ausgewertet werden soll. Dieser feste Bereich kann mit den Parametern ``Lower limit`` und ``Upper limit`` festgelegt werden. Wenn Region auf ``all`` gesetzt ist, wird der gesamte Frequenz-, bzw. Zeitbereich, zur Auswertung verwendet.
